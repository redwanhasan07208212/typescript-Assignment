## TypeScript ব্লগ পোস্ট

## ১. ইন্টারফেস এবং টাইপস - কোনটা কখন ব্যবহার করব?

TypeScript এ কাজ করতে গিয়ে অনেকে `interface` এবং `type` নিয়ে গুলিয়ে যায়। দেখতে পাঁচ পাঁচ মনে হলেও এদের মধ্যে কিছু ফারাক আছে যা জানা জরুরি।

### **Interface - অবজেক্টের জন্য বেশি উপযুক্ত:**

Interface মূলত অবজেক্টের স্ট্রাকচার বলে দেয়। যেমন একটি ইউজার অবজেক্টের কাছে কি কি প্রপার্টি থাকবে সেটা নির্ধারণ করি।

এক মজার কথা হলো - একই নামে দুবার `interface` লিখলে TypeScript নিজে থেকে সেগুলো একসাথে মিশিয়ে দেয়। খুব কার্যকরী!

```typescript
interface User {
  name: string;
  age: number;
}

interface User {
  email: string; // এটা আগেরটার সাথে মিশে যাবে
}

// এখন User এ তিনটি প্রপার্টিই থাকবে: name, age, email
```

### **Type - সব ধরনের কাজে লাগে:**

`Type` একটু বেশি ফ্লেক্সিবল। এর দিয়ে শুধু অবজেক্ট না, যেকোনো ধরনের টাইপ তৈরি করতে পারি। যেমন - union types বা primitive types।

```typescript
type Product = {
  id: number;
  name: string;
  price: number;
};

// আর এভাবে একাধিক অপশন থেকে একটা বেছে নেওয়ার টাইপও তৈরি করা যায়
type Status = "active" | "inactive" | "pending";
```

**খাঁটি কথা:** Interface হলো অবজেক্ট-ফোকাসড, Type হলো সার্বজনীন সিপাহি।

---

## ২. keyof কীওয়ার্ড - একটা লাইফ সেভার!

TypeScript এ কাজ করতে গিয়ে একটা দারুণ সমস্যা হয় - কোনো অবজেক্টের প্রপার্টির নাম ভুলে যাওয়া বা ভুল নাম দিয়ে ফেলা। এজন্য `keyof` রয়েছে!

`keyof` মানে হলো "এই টাইপের সব কী গুলো বের করে দাও"। এতে যা লাভ হয় - প্রোগ্রামার যদি ভুল কী দেয় তাহলে TypeScript সাথে সাথে এরর দেখায়।

### **একটা বাস্তব উদাহরণ:**

```typescript
interface Product {
  id: number;
  name: string;
  price: number;
}

// keyof দিয়ে বলে দিচ্ছি - শুধু এই তিনটি কী এর মধ্যেই হবে
type ProductKeys = keyof Product; // "id" | "name" | "price"

function getProperty(product: Product, key: ProductKeys) {
  return product[key];
}

const product = { id: 1, name: "Laptop", price: 5000 };
console.log(getProperty(product, "name")); // ✓ কাজ করবে
console.log(getProperty(product, "id")); // ✓ এটাও

// এখানে ভুল দিলে দেখুন কি হয়:
// getProperty(product, "color"); // ❌ Error! এটা Product এ নেই!
```

**কেন এটা গুরুত্বপূর্ণ:**

- কোডে কোনো টাইপো হলে সঙ্গে সঙ্গে ধরা পড়ে
- যখন প্রপার্টি বদলাই, সব জায়গায় অটোমেটিক আপডেট হয়
- কোডিং করার সময় IDE থেকে সাজেশন পাই

---

## মোটকথা

TypeScript শিখতে গিয়ে এই দুটো কনসেপ্ট বুঝে রাখলে অর্ধেক কাজ সেরে ফেলা। Interface আর Type কখন কোথায় ব্যবহার করবো এটা জানলে এবং keyof দিয়ে টাইপ-সেফটি নিশ্চিত করলে কোডিং অনেক সহজ এবং মজার হয়ে যায়!

হ্যাপি কোডিং! 🚀
